/**********************************************************************
 *
 *   Component generated by Quest
 *
 *   WARNING: By editing this component by hand, you will lose the ability to regenerate the code without conflicts.
 *   To preserve that ability, always export from Quest to regenerate this file.
 *   To setup element attributes and CSS properties, use the Quest web app
 *   Code Logic goes in the hook associated with this component
 *
 *   For help and further details refer to: https://www.quest.ai/docs
 *
 *
 **********************************************************************/

import React from 'react';
import { styled } from '@mui/material/styles';
import ProductBox1 from 'components/ProductBox1/ProductBox1';
import { ProdustListProps } from 'types';
import useProdustList from 'components/ProdustList/useProdustList';

const ProdustList1: any = styled('div')({
  display: `flex`,
  position: `relative`,
  isolation: `isolate`,
  flexDirection: `row`,
  width: '100%',
  height: `509px`,
  justifyContent: `flex-start`,
  alignItems: `flex-start`,
  padding: `0px`,
  boxSizing: `border-box`,
});

const ProductBox11: any = styled(ProductBox1)(({ theme }: any) => ({
  width: `311px`,
  position: `absolute`,
  left: `0px`,
  top: `0px`,
}));

function ProdustList(props: ProdustListProps): JSX.Element {
  const { data } = useProdustList();

  return (
    <ProdustList1 className={props.className}>
      {data.products &&
        data.products.map((product: any, index: number) => {
          return <ProductBox11 key={index} />;
        })}
    </ProdustList1>
  );
}

export default ProdustList;
