/**********************************************************************
 *
 *   Component generated by Quest
 *
 *   WARNING: By editing this component by hand, you will lose the ability to regenerate the code without conflicts.
 *   To preserve that ability, always export from Quest to regenerate this file.
 *   To setup element attributes and CSS properties, use the Quest web app
 *   Code Logic goes in the hook associated with this component
 *
 *   For help and further details refer to: https://www.quest.ai/docs
 *
 *
 **********************************************************************/

import React from 'react';
import { Dialog } from '@mui/material';
import { styled } from '@mui/material/styles';
import ProductPage from 'components/ProductPage/ProductPage';
import { ProductDialogProps } from 'types';
import useProductDialog from 'components/ProductDialog/useProductDialog';

const ProductDialog1: any = styled('div')({
  borderRadius: `0px`,
  display: `flex`,
  position: `relative`,
  isolation: `isolate`,
  flexDirection: `column`,
  justifyContent: `flex-start`,
  alignItems: `center`,
  padding: `0px`,
  boxSizing: `border-box`,
  height: 'auto',
  width: 'fit-content',
});

const ProductPage1: any = styled(ProductPage)(({ theme }: any) => ({
  width: `1440px`,
  margin: `0px`,
}));

function ProductDialog(props: ProductDialogProps): JSX.Element {
  const { data, fns } = useProductDialog();

  return (
    <Dialog open={data.isDialogOpen} onClose={fns.toggleDialog}>
      <ProductDialog1 className={props.className}>
        <ProductPage1 open={data.isDialogOpen} onClose={fns.toggleDialog} />
      </ProductDialog1>
    </Dialog>
  );
}

export default ProductDialog;
