/**********************************************************************
 *
 *   Component generated by Quest
 *
 *   WARNING: By editing this component by hand, you will lose the ability to regenerate the code without conflicts.
 *   To preserve that ability, always export from Quest to regenerate this file.
 *   To setup element attributes and CSS properties, use the Quest web app
 *   Code Logic goes in the hook associated with this component
 *
 *   For help and further details refer to: https://www.quest.ai/docs
 *
 *
 **********************************************************************/

import React from 'react';
import { styled } from '@mui/material/styles';
import Header from 'src/components/Header/Header';
import ModernWorkplace from 'src/components/ModernWorkplace/ModernWorkplace';
import ProductSectionSurfaceHubDevice from 'src/components/ProductSectionSurfaceHubDevice/ProductSectionSurfaceHubDevice';
import CustomerCases from 'src/components/CustomerCases/CustomerCases';
import Footer from 'src/components/Footer/Footer';
import Navbar from 'src/components/Navbar/Navbar';
import useMowoFun from 'src/components/MowoFun/useMowoFun';

const ScreenDesktop = styled('div')({
  display: `flex`,
  position: `relative`,
  isolation: `isolate`,
  flexDirection: `column`,
  width: '100%',
  justifyContent: `flex-start`,
  alignItems: `center`,
  padding: `0px`,
  boxSizing: `border-box`,
  overflow: `hidden`,
  height: 'auto',
});

const Header1 = styled(Header)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const ModernWorkplace1 = styled(ModernWorkplace)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const ProductSectionSurfac = styled(ProductSectionSurfaceHubDevice)(
  ({ theme }) => ({
    alignSelf: `stretch`,
    margin: `0px`,
  })
);

const CustomerCases1 = styled(CustomerCases)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const Footer1 = styled(Footer)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const Navbar1 = styled(Navbar)(({ theme }) => ({
  width: `1440px`,
  margin: `0px`,
}));

const Header2 = styled(Header)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const ModernWorkplace2 = styled(ModernWorkplace)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const ProductSectionSurfac1 = styled(ProductSectionSurfaceHubDevice)(
  ({ theme }) => ({
    alignSelf: `stretch`,
    margin: `0px`,
  })
);

const CustomerCases2 = styled(CustomerCases)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const Footer2 = styled(Footer)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const Header3 = styled(Header)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const ModernWorkplace3 = styled(ModernWorkplace)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const ProductSectionSurfac2 = styled(ProductSectionSurfaceHubDevice)(
  ({ theme }) => ({
    alignSelf: `stretch`,
    margin: `0px`,
  })
);

const CustomerCases3 = styled(CustomerCases)(({ theme }) => ({
  alignSelf: `stretch`,
  margin: `0px`,
}));

const Footer3 = styled(Footer)(({ theme }) => ({
  alignSelf: `stretch`,
  height: `504px`,
  margin: `0px`,
}));

function MowoFun(props) {
  const { data } = useMowoFun();

  return (
    <ScreenDesktop className={props.className}>
      {!(data.currentVariant === 'Tablet') &&
        !(data.currentVariant === 'ScreenMobile') && <Header1 />}
      {!(data.currentVariant === 'Tablet') &&
        !(data.currentVariant === 'ScreenMobile') && <ModernWorkplace1 />}
      {!(data.currentVariant === 'Tablet') &&
        !(data.currentVariant === 'ScreenMobile') && <ProductSectionSurfac />}
      {!(data.currentVariant === 'Tablet') &&
        !(data.currentVariant === 'ScreenMobile') && <CustomerCases1 />}
      {!(data.currentVariant === 'Tablet') &&
        !(data.currentVariant === 'ScreenMobile') && <Footer1 />}
      {!(data.currentVariant === 'Tablet') &&
        !(data.currentVariant === 'ScreenMobile') && <Navbar1 />}
      {data.currentVariant === 'Tablet' && <Header2 />}
      {data.currentVariant === 'Tablet' && <ModernWorkplace2 />}
      {data.currentVariant === 'Tablet' && <ProductSectionSurfac1 />}
      {data.currentVariant === 'Tablet' && <CustomerCases2 />}
      {data.currentVariant === 'Tablet' && <Footer2 />}
      {data.currentVariant === 'ScreenMobile' && <Header3 />}
      {data.currentVariant === 'ScreenMobile' && <ModernWorkplace3 />}
      {data.currentVariant === 'ScreenMobile' && <ProductSectionSurfac2 />}
      {data.currentVariant === 'ScreenMobile' && <CustomerCases3 />}
      {data.currentVariant === 'ScreenMobile' && <Footer3 />}
    </ScreenDesktop>
  );
}

export default MowoFun;
